package com.ech.backend.api.aiassistant.insight;

/** LLM conversation-insight job classification (JSON {@code suggestionKind}). */
public enum ConversationInsightKind {
    NONE,
    CALENDAR,
    WORKFLOW
}
