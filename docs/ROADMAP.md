# CSTalk 개발 로드맵

이 문서는 CSTalk 개발 진행의 기준 로드맵입니다.  
앞으로 작업은 본 문서를 기준으로 수행하며, 완료된 항목은 체크박스(`[v]`)로 처리합니다.

## 진행 규칙
- 새 작업 시작 전, 관련 로드맵 항목 존재 여부를 먼저 확인합니다.
- 완료된 작업은 삭제하지 않고 `[v]`로 체크합니다.
- 세부 작업이 생기면 하위 항목으로 추가합니다.
- 로드맵 상태가 변경되면 `.cursor/rules/CHANGELOG.md`에 함께 기록합니다.
- 기능 개발/수정 완료 시 `docs/FEATURE_SPEC.md`와 `docs/HANDOVER.md`를 함께 업데이트합니다.

> **운영 배포 기준선:** 데스크톱·백엔드 **`1.2.11`** (`v1.2.11`).

## Phase 1 - 기반 구축 (MVP Core)
- [v] 1-1. 프로젝트 실행 환경 안정화
  - [v] 1-1-1. Backend/Realtime/Frontend 로컬 실행 절차 검증
  - [v] 1-1-2. 환경 변수 표준(`.env.example`) 점검 및 문서화
- [v] 1-2. 기본 에러 처리/공통 응답 포맷 적용
  - [v] 1-2-1. `ApiResponse`/`ApiError` 공통 응답 모델 적용
  - [v] 1-2-2. `GlobalExceptionHandler` 기반 예외 매핑 적용
- [v] 1-3. PostgreSQL 스키마 초안 작성 (사용자/채널/메시지)
  - [v] 1-3-1. 핵심 테이블(`users`, `channels`, `channel_members`, `messages`) 정의
  - [v] 1-3-2. 인덱스/유니크/FK 정책 정의
- [v] 1-4. 채널 도메인 API 1차 구현 (`생성/조회/참여`)
  - [v] 1-4-1. 채널 생성 API (`POST /api/channels`)
  - [v] 1-4-2. 채널 조회 API (`GET /api/channels/{channelId}`)
  - [v] 1-4-3. 채널 참여 API (`POST /api/channels/{channelId}/members`)
  - [v] 1-4-4. 채널 개설자 멤버 내보내기 API (`DELETE /api/channels/{channelId}/members`) 및 멤버 패널 UI
- [v] 1-5. 실시간 메시지 저장 연계 (Socket 이벤트 + DB 저장)
  - [v] 1-5-1. Realtime 메시지 저장 리포지토리(`pg`) 구현
  - [v] 1-5-2. `message:send` 수신 시 DB 저장 로직 구현
  - [v] 1-5-3. 저장 성공 데이터만 `message:new`로 브로드캐스트

## Phase 2 - 협업 기능 확장
- [v] 2-1. 스레드 답글 기능 구현
  - [v] 2-1-1. 부모 메시지/자식 메시지 API 설계 및 구현
  - [v] 2-1-2. 스레드 조회 성능 인덱스 점검
- [v] 2-2. 읽음 상태/온라인 상태 관리 고도화
  - [v] 2-2-1. 읽음 포인터(채널별 마지막 읽은 메시지) 저장 · 타임라인 순 미읽음 집계 · `mark-latest-root` · 채널별 스크롤 비율(localStorage)
  - [v] 2-2-2. 사용자 Presence 상태 관리(온라인/자리비움/오프라인)
  - [v] 2-2-3. 채널/DM 목록 미읽음 건수 배지(API `unreadCount`·루트 메시지 기준·프론트 읽음 갱신)
  - [v] 2-2-4. 좌측 퀵 레일(미읽음·`lastMessageAt` 정렬·실시간)·사이드바 우측 돌출 탭 접기(`ech_sidebar_collapsed`)
- [v] 2-6. 채팅 @멘션(자동완성·실시간 알림·채널 이동)
  - [v] 2-6-1. 본문 토큰 `@{사번|표시명}`·`GET /api/users/search` 자동완성·렌더·토스트 UI
  - [v] 2-6-2. 채널 멤버 검증 후 `mention:notify`·Realtime `internal/notify-mentions`·Java `MentionNotificationService`
- [v] 2-3. 파일 업로드/다운로드 기본 기능 구현
  - [v] 2-3-1. 업로드 메타데이터 테이블 및 API 구현
  - [v] 2-3-2. 다운로드 권한/URL 발급 정책 구현
  - [v] 2-3-3. 프론트: 대용량(약 512KB+) 이미지 다운로드 시 원본/JPEG 압축 선택; DM 채팅 헤더에 프레즌스 점(사이드바 DM과 동일 규칙)
  - [v] 2-3-4. 이미지 업로드 시 서버 `preview` 저장·인라인 `/preview`·다운로드 `variant`·모달 용량 표시(구버전은 레거시 JPEG 경로)
  - [v] 2-3-5. 채팅 이미지 `blob:` URL 세션 캐시·LRU(채널 재입장·`loadMessages` 시 불필요 revoke 제거)
  - [v] 2-3-6. 백그라운드 동일 채널 일반 메시지 알림(`message:new`)·스레드 댓글 수 루트 집계
- [v] 2-4. 칸반 보드 도메인 1차 구현
  - [v] 2-4-1. 보드/컬럼/카드 CRUD API 구현
  - [v] 2-4-2. 카드 담당자/상태 변경 이력 기록
  - [v] 2-4-3. 채널 연동 기본 칸반 보드 조회/자동 생성 API 및 프론트 허브 UI 연동
- [v] 2-5. 채팅 메시지 -> 업무 항목 생성 연계
  - [v] 2-5-1. 메시지 ID 기반 업무 생성 API 구현
  - [v] 2-5-2. 메시지-업무 상호 링크 구조 설계
  - [v] 2-5-3. 채널 단위 업무 목록/생성/상태 변경 API와 채널 헤더 허브 UI 연동

## Phase 2.7 - 협업 인식 강화 (우선 여정·지식 축)

> **배경·갭 분석·AI/프로액티브/비용 원칙**은 [COLLABORATION_TOOL_DIRECTION.md](./COLLABORATION_TOOL_DIRECTION.md)에 정리. 본 Phase는 그중 **코드로 닫을 항목**만 체크한다.

- [ ] 2-7-1. 우선 여정 **「멘션 → 업무 → 마감 → 칸반」** 끊김 제거
  - [ ] 2-7-1-1. 업무 `due_at`(마감)·`priority`(우선순위) 도메인·API·워크 허브 UI
  - [ ] 2-7-1-2. **내 할 일** 뷰(오늘·지연·멘션 연계 업무) 또는 사이드바 확장
  - [ ] 2-7-1-3. 마감 임박·지연 **업무 단위** 알림/배지(메시지 미읽음과 구분되는 정책)
  - [ ] 2-7-1-4. 멘션 알림·메시지에서 **업무 생성 → 칸반 배치** 최소 클릭 연계 강화
- [ ] 2-7-2. 지식 축 1차 — **채널 자료실** (위키/페이지는 후속 Phase)
  - [ ] 2-7-2-1. 채널 고정 **폴더·핀** (기존 파일 허브 확장)
  - [ ] 2-7-2-2. 파일 **설명·태그** 메타, **메시지 맥락 묶음**(해당 스레드/메시지 첨부 모음 링크)

## Phase 3 - 관리/보안/운영
- [v] 3-0. 사용자 인증(JWT 로그인) 구현
  - [v] 3-0-1. JWT 발급/검증 유틸 및 Spring Security 필터 구성
  - [v] 3-0-2. AuthProvider 인터페이스 및 테스트 인증 구현체(TestAuthProvider)
  - [v] 3-0-3. 로그인 API (`POST /api/auth/login`) 및 내정보 API (`GET /api/auth/me`)
  - [v] 3-0-4. 프론트엔드 로그인 화면 및 JWT 자동 첨부
  - [v] 3-0-5. DataInitializer: 서버 기동 시 비밀번호 미설정 계정에 기본값 자동 적용
- [v] 3-1. 조직도/사내 계정 연동 인터페이스 정의
  - [v] 3-1-1. 사용자 동기화 배치/이벤트 연동 방식 결정
  - [v] 3-1-2. 계정 활성/비활성 정책 정의
  - [v] 3-1-3. 조직도 속성(부서) 기반 사용자 검색 API 제공
  - [v] 3-1-4. `users` 슬림화(조직 컬럼 제거) 및 `org_group_members`를 `users.employee_no` FK로 통일, 직급/직위/직책 그룹 타입(`JOB_LEVEL`/`JOB_POSITION`/`JOB_TITLE`) 적용
- [v] 3-2. RBAC 세분화 및 관리자 권한 정책 반영
  - [v] 3-2-1. 역할-권한 매트릭스 문서화
  - [v] 3-2-2. API 단 권한 체크 미들웨어/어노테이션 반영
- [v] 3-3. 감사 로그 수집 체계 구축
  - [v] 3-3-0. 오류 로그(error_logs) 저장/조회 기반 구축
  - [v] 3-3-1. 채널/메시지/파일/업무 이벤트 감사 로그 저장
  - [v] 3-3-2. 관리자 조회 API 및 필터(기간/사용자/행위) 구현
- [v] 3-4. 보존 정책 및 아카이빙 기본 정책 구현
  - [v] 3-4-1. 만료 데이터 아카이빙 스케줄러 구현
  - [v] 3-4-2. 보존 정책 설정값 관리 기능 구현
- [v] 3-5. 관리자 업그레이드/배포 관리 기능 구현
  - [v] 3-5-1. 관리자 릴리즈 업로드 API/화면 구현 (WAR 업로드)
  - [v] 3-5-2. 버전 전환(활성화) API/화면 구현
  - [v] 3-5-3. 롤백 API/화면 구현 및 배포 이력 기록
  - [v] 3-5-4. 업그레이드/롤백 감사 로그 연동
- [v] 3-6. 검색 인덱스/통합 검색 1차 구현
  - [v] 3-6-1. 메시지/파일/업무 공통 검색 API 구현
  - [v] 3-6-2. 검색 정확도/성능 튜닝 기준 수립
  - [v] 3-6-3. 통합 검색 범위 확장(채널명/댓글 검색 타입 추가)

## Phase 4 - 안정화 및 품질
- [v] 4-1. API/소켓 통합 테스트 시나리오 정리
  - [v] 4-1-1. 핵심 사용자 여정(E2E) 테스트 케이스 작성
  - [v] 4-1-2. 자동화 테스트 파이프라인 연동
- [v] 4-2. 성능 점검 (메시지 지연, 동시 접속 기준)
  - [v] 4-2-1. 부하 테스트 스크립트 작성
  - [v] 4-2-2. 병목 구간 분석 및 개선
- [v] 4-3. 장애/재시도 전략 검증
  - [v] 4-3-1. 서버 재시작/네트워크 단절 시나리오 검증
  - [v] 4-3-2. 메시지 유실 방지 및 재처리 전략 검증
- [v] 4-4. 운영 문서/배포 문서 정리
  - [v] 4-4-1. 배포 절차/롤백 절차 문서화
  - [v] 4-4-2. 모니터링/알람 기준 문서화

## Phase 5 - AD 인증 / 사용자 관리
- [v] 5-1. AD 자동 로그인 (Electron 전용)
  - [v] 5-1-1. Electron main.js IPC `get-windows-username` (os.userInfo 기반)
  - [v] 5-1-2. preload.js `electronAPI.getWindowsUsername` 노출
  - [v] 5-1-3. 백엔드 `POST /api/auth/ad-login` 구현 (사원번호 존재+ACTIVE 검증 후 JWT 발급)
  - [v] 5-1-4. 기존 일반 로그인에 INACTIVE 계정 차단 로직 추가
  - [v] 5-1-5. 프론트 `tryAdAutoLogin()` — 앱 초기화 시 AD 자동 로그인 시도
- [v] 5-2. 관리자 사용자 관리 기능
  - [v] 5-2-1. 백엔드 `GET /api/admin/users` — 전체 사용자 + 조직 정보 조회
  - [v] 5-2-2. 백엔드 `GET /api/admin/users/org-options` — 부서/직급/직위/직책 드롭다운
  - [v] 5-2-3. 백엔드 `POST /api/admin/users` — 사용자 등록
  - [v] 5-2-4. 백엔드 `PUT /api/admin/users/{employeeNo}` — 사용자 정보/상태/조직 수정
  - [v] 5-2-5. 백엔드 `DELETE /api/admin/users/{employeeNo}` — 하드 삭제
  - [v] 5-2-6. 프론트 사이드바 관리자 메뉴에 "👤 사용자 관리" 추가
  - [v] 5-2-7. 프론트 사용자 관리 페이지 (테이블 + 편집 모달 + 배치 저장)

## Phase 6 - 개인 캘린더·공유 (AI 비서 연동 전제 동시 설계)

> **원칙:** 캘린더는 추후 그룹웨어·Google 연동을 염두에 두되, **처음부터** AI 비서가 읽기·제안·**사용자 확정 후** 반영할 수 있도록 **제안(초안) 엔티티 / 확정 API / 출처·행위자(actor) / 감사**를 사람용 기능과 동일 축으로 설계한다. UI는 [docs/FEATURE_SPEC.md](FEATURE_SPEC.md)의 허브·테마와 동화(별도 앱 느낌 지양).  
> **플랜 대비 요약:** [COLLABORATION_TOOL_DIRECTION.md](./COLLABORATION_TOOL_DIRECTION.md) §3.

- [v] 6-1. 사용자당 단일 캘린더 및 직접 일정
  - [v] 6-1-1. 사용자 1인 1캘린더 도메인·스키마(API: 내 캘린더 조회·직접 일정 CRUD)
  - [v] 6-1-2. 프론트: 워크 허브·채널 헤더 등 기존 패턴에 일정 뷰/상세 진입(테마·구역 훅 정합)
- [v] 6-2. 채널·DM 일정 공유 및 수락
  - [v] 6-2-1. 채널에서 특정 사용자에게 일정 공유 요청·수락/거절·만료(멤버 권한 검증)
  - [v] 6-2-2. DM에서 동일 공유·수락 흐름(DM 참여자만; **프로액티브 AI 자동 관찰**과 별개인 사용자 주도 행위)
  - [v] 6-2-3. 수락 시 수신자 본인 달력에 반영(복사본 1차·발신 수정 동기화는 2단계로 분리 가능)
- [v] 6-3. 출처(프로비넌스) 및 감사
  - [v] 6-3-1. 공유로 수신된 일정에 채널 또는 DM 출처 표시(가능 시 메시지·링크 연계)
  - [v] 6-3-2. 공유/수락/거절·일정 확정을 감사 로그에 반영
- [ ] 6-4. AI 비서와의 접점(캘린더 MVP 범위 내)
  - [ ] 6-4-1. `CalendarSuggestion`(가칭) 등 **초안 vs 확정** 분리·확정 시 **직접 생성과 동일 API** 경로
  - [ ] 6-4-2. 이벤트·제안에 `originChannelId` / `originDmChannelId` / `originMessageIds` / `createdByActor`(USER|AI_ASSISTANT) 등 메타
  - [ ] 6-4-3. AI가 타인에게 일정을 넣는 제안도 **공유→수락** 모델 우회 금지
  - [ ] 6-4-4. 권한 내 **바쁨/내 일정 조회·충돌 검사** API(LLM 툴 호출·배치가 동일 엔드포인트 사용)
- [ ] 6-5. (선택) iCal 가져오기/보내기 — 외부 연동 Phase 이전 단계로 가치 있음

## Phase 7 - AI 비서 (게이트웨이·프로액티브·비용)

> **원칙:** LLM 호출은 백엔드 **게이트웨이**에서 권한·레이트리밋·감사 일원화. 채널 **옵트인** 프로액티브는 **DM 자동 관찰 제외**(합의). 방해 최소화를 위해 **제안함**·쿨다운·신뢰도 임계값·다이제스트 옵션·2단(규칙/소형→LLM) 트리거·토큰 예산을 적용.  
> **데이터 주권:** 협업 **원문(채널·DM·첨부)** 은 **기본 비유출**—공용 인터넷 LLM으로 **기본 전송하지 않음**. 추론·RAG는 **온프레/전용망·VPC 전용 추론** 또는 **계약상 미학습·보존 배제**가 명시된 경로만 예외로 검토(법무·보안 합의).  
> **정책·범위·비용 지표 상세:** [COLLABORATION_TOOL_DIRECTION.md](./COLLABORATION_TOOL_DIRECTION.md) §4~§7.

- [ ] 7-1. AI 게이트웨이 및 공통 거버넌스
  - [ ] 7-1-0. **기본 비유출** 정책 문서화·게이트웨이 기본 차단·예외(전용망·전용 테넌트) 승인 절차·임베딩/RAG 경계
  - [ ] 7-1-1. LLM 호출 API·요청/응답 메타 감사·PII 마스킹 정책(온프레/클라우드 모델 선택지 문서화)
  - [ ] 7-1-2. 환각 완화: 근거 `message_id` 인용·편집 후 전송 UX
  - [ ] 7-1-3. (선택) RAG: 권한 필터 이후 임베딩·검색
- [ ] 7-2. 대화 기반 보조 기능
  - [ ] 7-2-1. 채널/스레드 요약·답장 초안·통합검색 연계 Q&A
  - [ ] 7-2-2. 메시지·업무·칸반과 연계한 자연어 초안(기존 API와 결합)
- [ ] 7-3. 프로액티브 비서 및 제안함
  - [ ] 7-3-1. 채널 단위 옵트인·DM 프로액티브 비관찰·사용자 톤(조용함/균형/적극)
  - [ ] 7-3-2. 제안함 큐(업무·일정 공통)·시간당 상한·거절 후 쿨다운·다이제스트 모드
  - [ ] 7-3-3. 확인 후 변이(mutating) 작업만 실행(업무 생성·일정 확정 등)
- [ ] 7-4. 비용·토큰 통제
  - [ ] 7-4-1. 컨텍스트 캡·윈도우 해시 캐시·배치 호출
  - [ ] 7-4-2. 워크스페이스/일 단위 쿼터·초과 시 폴백(규칙 전용 등)·관측 대시보드

## Phase 8 - 외부 캘린더 연동 (그룹웨어·Google 등)

> **원칙:** 내부 도메인(`Calendar`/`Event` 등)과 **CalendarConnector** 경계로 공급자 플러그인화. ECH **1인 1달력**을 내부 스케줄 레이어로 유지할지·외부 SSOT와 병합할지는 연동 단계에서 정책 확정.

- [ ] 8-1. 커넥터 추상화 및 인증
  - [ ] 8-1-1. Google / Microsoft 365 / 사내 그룹웨어(Caldav·전용 API) 연결 인터페이스 설계
  - [ ] 8-1-2. OAuth·스코프·토큰 저장·갱신
- [ ] 8-2. 동기화 단계
  - [ ] 8-2-1. 읽기 동기화(바쁨·이벤트 표시)
  - [ ] 8-2-2. 생성·수정·취소(권한·감사·충돌·타임존·반복 규칙)
- [ ] 8-3. AI·프로액티브와의 관계
  - [ ] 8-3-1. 외부 일정은 **표시 합성** 우선, AI 확정 쓰기는 정책에 맞게 허용 범위 명시
